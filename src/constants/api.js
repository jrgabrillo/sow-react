import {sendRequest} from '../api/Base';

export const searchMap = (query) => {
  return sendRequest({
    base: ``,
    path: ``,
    method: 'GET'
  });
}

export const getCities = (query) => {
  return sendRequest({
    base: ``,
    path: ``,
    method: 'GET'
  });  
}
